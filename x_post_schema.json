{
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "reason", "posts"],
  "properties": {
    "format": {"type": "string", "enum": ["single", "thread"]},
    "reason": {"type": "string"},
    "posts": {
      "type": "array",
      "minItems": 1,
      "maxItems": 3,
      "items": {"type": "string", "minLength": 1, "maxLength": 280}
    }
  }
}
